// jquery document iniatlize and page load actions
$(document).ready(function() {
console.log("Page loaded, JS started")

//set the DB
var database = firebase.database();

//create the initial variables and settings

//add a new train scheudule to the DB
Train Name, Destination, First Trip (HH:MM), Frequency in Minutes


//display the train schedule
---> Train schedule -- 
Train Name
Destination
Frequency (MIN)
Next Arrival
Minutes Away


//End the document ready function
});